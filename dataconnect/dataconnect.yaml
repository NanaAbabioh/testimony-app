specVersion: "v1"
serviceId: "testimony-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "testimony-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./example"]
